# file: /etc/fail2ban/jail.d/ssh.conf
# Used to configure a Fail2Ban jail for SSH

[sshd]
enabled  = true
port = {{ ubuntu_common_ssh_port }}
filter = sshd
action = ufw[name=SSH, port=ssh, protocol=tcp]
logpath = %(sshd_log)s
maxretry = 5 ;# Override the default of 3

[sshd-ddos]
port    = {{ ubuntu_common_ssh_port }}
action = ufw[name=SSH, port=ssh, protocol=tcp]
logpath = %(sshd_log)s
